import {Meta} from '@storybook/addon-docs';
import Rectangle from './utils/Rectangle'; // Ajusta la importación según tu estructura
import {Color} from '../themes/color';
import {GridSection} from './utils/GridSection';

<Meta title="Fanatics/Colors"/>

# Colors

### Primary Colors

<GridSection colors={Object.entries(Color).slice(10, 20)}/>

### Secondary Colors

<GridSection colors={Object.entries(Color).slice(20, 30)}/>

### Tertiary

<GridSection colors={Object.entries(Color).slice(30, 40)}/>

### Text

<GridSection colors={Object.entries(Color).slice(40, 53)}/>

### Icons

<GridSection colors={Object.entries(Color).slice(53, 55)}/>

### Surface

#### Backgrounds

<GridSection colors={Object.entries(Color).slice(55, 69)}/>

#### Border and Drivers

<GridSection colors={Object.entries(Color).slice(69, 72)}/>

### Functional
#### Success

<GridSection colors={Object.entries(Color).slice(72, 82)}/>

#### Warning

<GridSection colors={Object.entries(Color).slice(82, 92)}/>

#### Error

<GridSection colors={Object.entries(Color).slice(92, 102)}/>

#### Info

<GridSection colors={Object.entries(Color).slice(102, 112)}/>

### Core Ant Colors

#### Dust Red

<GridSection colors={Object.entries(Color).slice(112, 122)}/>

#### Volcano

<GridSection colors={Object.entries(Color).slice(122, 132)}/>

#### Sunset Orange

<GridSection colors={Object.entries(Color).slice(132, 142)}/>

#### Calendula Gold

<GridSection colors={Object.entries(Color).slice(142, 152)}/>

#### Sunrise Yellow

<GridSection colors={Object.entries(Color).slice(152, 162)}/>

#### Lime

<GridSection colors={Object.entries(Color).slice(162, 172)}/>

#### Polar Green

<GridSection colors={Object.entries(Color).slice(172, 182)}/>

#### Cyan

<GridSection colors={Object.entries(Color).slice(182, 192)}/>

#### DayBreak Blue

<GridSection colors={Object.entries(Color).slice(192, 202)}/>

#### Geek Blue

<GridSection colors={Object.entries(Color).slice(202, 212)}/>

#### Golden Purple

<GridSection colors={Object.entries(Color).slice(212, 222)}/>

#### Magenta

<GridSection colors={Object.entries(Color).slice(222, 232)}/>
